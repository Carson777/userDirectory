<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Mustache Activity</title>
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>

    <div class="container">
      <div class="row">
        <div class="col-xs-12">
        <div class="profile">
            <div class="top">
              <div class="top-left">
                <img class="avatar" src="{{user.avatar}}">
              </div>
              <div class="top-right">
                <div class="title">
                  <h3 class="fullName">{{user.name}}</h3>
                </div>
                  <p> {{user.address.city}}, {{user.address.country}} | {{user.job}}, {{user.company}} </p>
                  <p>{{user.email}} | {{user.phone}}</p>
              </div>
            </div>
            <div class="bottom">
              <p>Education: {{user.education}}</p>
              <p>Skills: {{user.skills}}</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>

<!-- Latest compiled and minified JavaScript -->
